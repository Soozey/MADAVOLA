# Copier en .env. L'API charge ../../.env (racine) pour POSTGRES_*, puis ce fichier.
# DATABASE_URL est construit depuis POSTGRES_USER, POSTGRES_PASSWORD, etc. du .env racine.
# Optionnel : DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/madavola
JWT_SECRET=change_me_jwt_secret_key_min_32_characters_long_for_jwt
